using system;
namespace CSharpFundamentals
{
    public enum ShippingMethod
    {
        RegularAirMail=1,
        RegularAirMail=2,
        Express=3
    }
    Class Program
    {
        Static Void Main (String[]args)
        {
            Var method = Shippimg method. Express;
            Console.WriteLine((int)method);
        }
    }
}